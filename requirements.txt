# ============================================================
# Trade-CLI — Dependencies
# Fase 2 — 2026-05-01
# ============================================================

# ── CLI & Terminal ─────────────────────────────────────────
typer[all]==0.12.3
rich==13.7.0
click==8.1.7

# ── Configuration ──────────────────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.1

# ── Data & Analysis ────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.4
ta>=0.11.0

# ── HTTP ───────────────────────────────────────────────────
httpx>=0.27.0
requests>=2.31.0

# ── Database ───────────────────────────────────────────────
# SQLite vem embutido no Python — sem dependência extra

# ── Knowledge & RAG ────────────────────────────────────────
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0

# ── Text Processing (training) ─────────────────────────────
pypdf>=4.0.0
markdown>=3.6
python-dateutil>=2.9.0

# ── Logging ────────────────────────────────────────────────
structlog>=24.1.0

# ── Scheduling ─────────────────────────────────────────────
schedule>=1.2.1

# ── Testing ────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0

# ── Code Quality ───────────────────────────────────────────
ruff>=0.4.0
mypy>=1.10.0

# ── MT5 Integration (Windows only — descomentar quando necessário) ──
# MetaTrader5>=5.0.45

# ── Ollama Python Client (opcional — usamos httpx directo) ─
# ollama>=0.2.0
